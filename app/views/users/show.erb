<h1 id="welcome">Welcome, <%= @user.username %>!</h1>
<br>
<h2 id="favorites">* Favorited Restaurants *</h2>

<ul>
  <% @user.favorited_restaurants.each do |restaurant| %>
  <br>
      <li>
        <a href="<%= restaurant.url %>"><%= restaurant.name %></a>
      </li>
  <% end %>
</ul>
